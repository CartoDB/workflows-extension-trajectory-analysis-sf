# Compute Metrics

Computes derived metrics for segments (consecutive points) within trajectories, including distances, durations, directions, speeds, and accelerations of specified units. These metrics are included as a new property in the properties section of the trajectory column.